# 前置测试大纲 (Shift-Left Test Outline) - 地毯式覆盖版

> 以下为 `docs/05-前置测试大纲/ASSET-ALL.md`。必须 100% 验证以下全部的主干线逻辑及基础防崩溃边界，且前端必须逐一留存截图凭证后方可通过门禁允许提交。本稿件已对齐 ASSET 001-005 的每一条红线要求进行双向用例生成。

## 1. 考纲范围与定位说明
- **发卷日期**：2026-03-23
- **关联需求**：`docs/03-需求文档/ASSET-001 ~ 005.md`

---

## 2. 必考题：正向业务场景 (Happy Path)
> 严格对齐需求的每条 AC，正向业务流的极尽穷举。

| 模块 | 用例编号 | 对应 AC | 场景特征 | Given (状态) | When (触发) | Then (预期) | 实机截图凭证 |
|:---|:---|:---|:---|:---|:---|:---|:---|
| **001分类目录** | TC-001-01 | 001/AC01 | **分类与目录建置及层级计算** | 空库 | 新建 Category A，接着建 Catalog A1(挂A)，再建 A2(挂A1) | API返回录入成功，A1 的 level 自动算出为 1，A2 为 2。 | [树形结构3层级联展开图] |
| **001分类目录** | TC-001-02 | 001/AC03 | **全量 CSV 双端吞吐导出导入** | 已有 3 层复杂目录架构 | 点击分类管理菜单的`...`->导出CSV，清空本地库后重新通过菜单导入该 CSV | 界面树形目录原样重新构建复原，所有 Order 序号毫厘不差 | [导入成功后的满屏幕树图] |
| **001分类目录** | TC-001-03 | 001/AC04 | **UI对齐与核心操作悬浮件** | 进入分类页 | 鼠标悬浮并注视右侧主版面顶端 | 必须 100% 具备复刻自术语库的 UpVote、版本史和包含`导出/导入/重命名/删除`的`...`菜单 | [右上角五大按键高清截图] |
| **001分类目录** | TC-001-04 | 001/AC05 | **目录详情页全域子孙资产聚合** | 目录A下有A1,A2，A1有资产X，A2有资产Y | 点击根目录 A 的“数据资产”Tab | 表格必须罗列出所有直系与旁系后代的资产，即同时列出 X 和 Y | [包含聚合出的 X 和 Y 列表展示图] |
| **002类型字典** | TC-002-01 | 002/AC01 | **四大规范字典底座装配** | - | 创建 Attribute `cost`，选归口为`业务`，类型为`数值`，设定只准`Role-Admin`修改，不勾必填 | 字典清单成功长出此骨干特征体 | [带4个枚举列的字段表格截图] |
| **002类型字典** | TC-002-02 | 002/AC02 | **单一字典多模版并行复用交叉** | 已有 Attribute `cost` | 分别在 Type-A 和 Type-B 的管理页点击“添加属性”并均勾选 `cost` | 两边均绑定成功，在各自的详情页均能看见该字段的存在且无主键冲突 | [Type-A与B各自含有此属性的并排图] |
| **002类型字典** | TC-002-03 | 002/AC03 | **分类页UI像素级吻合检验** | 查看类型主页 | 仔细对比 | 左清单右详情，整体布局和“分类(Classification)”原生件绝对共轨 | [左右界面大对比图] |
| **002类型字典** | TC-002-04 | 002/AC04 | **依据领域归口引发的动态切片 Tab** | Type-A 身上绑了“基础信息”和“技术信息”两个领域的共5个属性 | 在清单中选中 Type-A 查看详版 | 下方大视图自动分解且仅分解为“基本信息”，“技术信息”，“活动信息流”三个大 Tab 按键 | [精确切分出的三个 Tab 头长截图] |
| **003数据填报** | TC-003-01 | 003/AC01 | **悬空目录落户容错录入** | 新建数据资产 | 故意把“资产目录”选框留白，但“资产类型”选定某一合法类型并提交 | 数据库容忍无归属地的悬垂叶子节点，成功生成 DataAsset 返回 200 | [带有空目录标签保存成功的 Toast 界面] |
| **003数据填报** | TC-003-02 | 003/AC02 | **前台面向实例表单动态展平拼接** | Type-A 的“基础信息”栏含有3个字符+1个日期 | 进入该Type派生的某资产编辑页，切到“基本信息”Tab | 该面板下必然依次竖排出现 3 个 Text Input 与 1 个 DatePicker，且数据绑定准确实时持久化 | [展现表单与日期拾取器的动态表单框图] |
| **003数据填报** | TC-003-03 | 003/AC04 | **多重三头组合导航视图大筛** | 进入数据资产主列表区 | 输入搜索“Test”，类型筛“Type-A”，目录筛“Catalog-X” | 三个条件化为复合 Query 发送给后台并实现精准的 3 维求交集返回对应结果 | [具备上方三行条件过滤栏的表格展现] |
| **004大盘搜索** | TC-004-01 | 004/AC01 | **Explore 级全屏分裂互动探洞** | 从侧边栏点击概览 | 单击罗列出的资产名称 | 左边保持长列表原状，右边向内挤压拉出类 Preview 的侧边抽屉展板呈现该资产宏观 | [左表右预览的大双轨联动截图] |
| **005公共底层** | TC-005-01 | 005/AC02 | **国际化语系倒转彻查无死角** | 英文系统，含有数万条标签名 | 头像处切换系统语言到简体中文 | 资产模块哪怕犄角旮旯的描述提示也全部切为精准生动的中文，无一处透出原生英文或 `xx.label.xxx` | [中文化无死角的概览大图] |
| **005公共底层** | TC-005-02 | 005/AC03 | **全局高光顶栏穿透发现** | 各个角落瞎逛 | 点击 OM 最顶端的全局大搜索栏，敲入刚才建的新 Catalog 名 | 像搜索表和库一样，顶流接口能将其下拉高亮回显出来捕捉成功 | [全局导航检索栏含结果下推图] |

---

## 3. 必考题：逆向与健壮性防护 (Boundary & Exception)
> 同样严格对齐需求的每条 AC 的破坏形态反向护城河映射。

| 模块 | 用例编号 | 对应 AC | 破坏场景 | Given (状态) | When (极限操作) | Then (防御预期) | 实机截图凭证 |
|:---|:---|:---|:---|:---|:---|:---|:---|
| **001分类目录** | TC-E001-01 | 001/AC02 | **目录携挂实体防自毁拦截网** | 某目录X下挂载有资产实例 | 点击UI或调API对X触发删除，输入DELETE | 严办，前端/后端皆抛出包含中文“仍有关联绑定残”红字 409 Conflict 拦截防击穿。 | [包含精确拦截明文的弹层截图] |
| **001分类目录** | TC-E001-02 | 001/AC04 | **误触防御 DELETE 输入保护机制** | 删除空目录 | 呼出删除模态确认框 | 如果未在输入框一字不差键入 "DELETE" 六个大写英文字母，红色毁灭按钮处于置灰未激活状态无法按压。 | [确认框置灰态验证图] |
| **003数据资产** | TC-E003-01 | 003/AC01 | **空骨架建档绝对拦截门限** | UI建档表单 | 把“资产类别 (AssetType)”这唯一定位字段清空尝试落库 | 页面前置强必填报错阻拦 / API 报 400 Bad Request 字段缺失。不容许无模板无家可归数据横空出世。 | [Type选框爆红提示必填警告截存] |
| **003数据资产** | TC-E003-02 | 003/AC03 | **JSON 扩展越界越权物理阻断窗** | 设定字段`phone`限 `Admin` 篡改 | 使用只含普通职级的 Token | 1. 界面上该字段旁不暴露编辑小铅笔。 2. 爬包抽出写口强行发送 PATCH 向 `extension_data` 覆写该段。 | 接口响应明确 403 Forbidden 并含有“权限拒绝”中文字眼，该字段数据库纹丝不动。 | [403拦截抓包+UI界面无铅笔图双证] |
| **004大盘搜索** | TC-E004-01 | 004/AC04 | **纯粹实体大盘抗杂质污染核查** | 大盘ES拉取清单 | 库内存着上万个 `AssetAttribute` 和 `AssetCatalog` 配置实体。 | 主概览的只展现 `DataAsset` 最终结果行，任何配角字典或中间态对象不容许漏嘴串台出现在这个给最终消费者的货柜面板上。 | [搜索模糊字但结果绝对清澈干净只有资产实体的截图] |
| **005公共底线** | TC-E005-01 | 005/AC01 | **后端文件目录污染约束抽查验证** | Review 或 编译期 | 发动扫包扫描该 Feature 的相关 controller 与 entity 对象。 | 不见任何一个 Java Class 或是 JS Page 文件飞溅到 `asset` 限定领域范畴目录以外的通用包体中发烂发臭。 | [代码目录树组织架构截图] |
| **005公共底线** | TC-E005-02 | 005/AC05 | **逻辑死尸绝不留守真·物理清除令** | 有一个测试目录 Y | 正常在 UI 上完成了带语境确认的删除流程令其灰飞。 | 登录底层 RDBMS 数据库操控台，`select * from asset_catalog where id='Y'`。必须是实打实的 Empty Set (0 Rows)，如果能查出所谓 deleted=1 就是零分打回发配。 | [终端数据库空集返回图] |
