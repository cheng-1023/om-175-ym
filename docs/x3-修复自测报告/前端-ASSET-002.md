# 前端修复自测报告: ASSET-002

## 修订历史
| 轮次 | 发现问题 | 修复措施 | 重测结果 |
|------|----------|----------|----------|
| 第1轮 | 1. 创建资产目录时，由于分类 API 没索取 FQN，引发 Payload 空字符导致后端校验/创建时 409 异常。2. 前端 409 拦截不够具体，依然报无业务意义的错误兜底“提交失败，请重试” | 增加 `|| selectedCategory?.name` 补齐字段；拦截 Axios HTTP 409 和 400，提取错误并显示针对性多语言异常。 | ✅ 通过 |

## 考题应答与核对清单
1. 验证创建 Catalog 时，如果 `selectedCategory.fullyQualifiedName` 缺失，是否成功 Fallback 到 `name`？
   - ✅ 验证通过。已观测到 `|| selectedCategory?.name` 生效。
2. 验证出现同名冲突时，界面是否可以友好接管并展示 Toast 提示，而不是将异常扔给堆栈或者泛化报错？
   - ✅ 验证通过。当 409 StatusCode 返回时，正确显示 `该资产目录已存在或者名称冲突`，有效阻止双重数据事故。

## 客观凭证
- [前端本地编译规范无报错日志校验](./前端-ASSET-002-附件/lint-result.txt)
